# 外部事實查證記錄(qvault)

| 事實點 | 聲稱值 | 驗證來源 | 驗證結果 | 日期 |
|---|---|---|---|---|
| P1 的 ML-KEM-1024 是否需要新增一個 PQC 相依 | **不需要**。`cryptography` 已內建 `hazmat.primitives.asymmetric.mlkem`,提供 `MLKEM1024PrivateKey` / `MLKEM1024PublicKey`(另有 `mldsa`) | 2026-09-09 於 asp-ng 開發機實跑:`MLKEM1024PrivateKey.generate()` → 公鑰 **1568** bytes;`encapsulate()` 回 `(shared_secret 32, ciphertext 1568)`;`decapsulate()` 與封裝端**共享秘密一致**;竄改密文一 byte → **隱式拒絕**(回不同秘密而非報錯,符合 FIPS 203 設計)。三個長度逐項對上 FIPS 203 ML-KEM-1024 規格 | ✅ **ROADMAP P1 的「能力上限」是過期資訊**。原文寫「需先擴 asp-ng 沙箱(worker 映像烘 PQC 庫並 pin)——在此之前 loop 建不了,故為觸發式」;實測 worker 映像 `asp-ng-worker:dev` 內 `cryptography` **50.0.0**,ML-KEM-1024 直接可用。**觸發條件已達成,只是沒人去看** | 2026-09-09 |
| `cryptography` 從哪一版起有 ML-KEM | 加入於 **47.0.0**(2026-04-24),當時標記 experimental 且需 AWS-LC / BoringSSL;**48.0.0**(2026-05-04)起隨標準 wheel 提供(需 OpenSSL 3.5.0+) | 2026-09-09 取 `https://cryptography.io/en/latest/changelog/` 逐條 | ⚠️ **API 仍標記 experimental**。對「只用受審密碼庫、永不手刻」的鐵律而言,這是**可用但介面可能變動**——不是實作品質問題(底層是 AWS-LC/BoringSSL/OpenSSL 的 ML-KEM),而是 Python 這一層的 API 穩定性問題。採用時應把版本下限釘死並在測試裡固定 KAT,使介面變動當場紅 | 2026-09-09 |
| worker 映像的 `cryptography` 是怎麼進去的 | **透過 `pyjwt[crypto]>=2.8` 間接拉進來,沒有任何地方宣告或 pin `cryptography`** | 2026-09-09 讀 asp-ng `pyproject.toml`:`dependencies = ['pyyaml>=6', 'pyjwt[crypto]>=2.8']`——`cryptography` 不在其中;而 `docker/worker.Dockerfile:87` 是從該檔解出相依再 `pip install` | 🔴 **qvault 的整個密碼核心站在一個沒人宣告的間接相依上**。映像目前是 50.0.0 純屬解析當下的結果;重建映像可能拿到別的版本,而**沒有任何東西會說「ML-KEM 不見了」**。P1 開工前應在 asp-ng 明確宣告 `cryptography>=48`,並加一條映像能力測試 | 2026-09-09 |
